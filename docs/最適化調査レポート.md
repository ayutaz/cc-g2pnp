# CC-G2PnP 最適化調査レポート

**調査日**: 2026-02-28
**調査方法**: 10 エージェントによる並列深層調査
**目的**: FlashAttention 以外の最適化ポイントの包括的な調査

---

## 1. エグゼクティブサマリー

### 即実装可能な高インパクト最適化 (TOP 5)

| # | 施策 | 効果 | 実装コスト |
|---|------|------|-----------|
| 1 | **PnP ラベル事前キャッシュ (LMDB)** | GPU 利用率 10-15% → 70-80% | 中 (前処理スクリプト) |
| 2 | **`fused=True` (AdamW)** | optimizer step 37% 高速化, 1.2M steps で ~5h 削減 | 1 行変更 |
| 3 | **`expandable_segments:True`** | CUDA フラグメンテーション大幅低減 | 環境変数 1 行 |
| 4 | **`repeat_interleave` → `expand+contiguous`** | embedding upsample 1.38x 高速化 | 1 行変更 |
| 5 | **`torch.inference_mode()` + FP16 推論** | 推論速度 +50-100% | 数行変更 |

> **Note**: Phase 0 (ゼロコスト改善 7 項目) は 2026-02-28 に全項目実装完了。コミット `bce295d`。
> **Note**: Phase 1 (低コスト改善 7 項目) は 2026-02-28 に全項目実装完了。コミット `d37a34b`。テスト数: 499 件 (Phase 1-5 + FlashAttention SDPA)。
> **Note**: Phase 2 (中コスト改善 5 項目) は 2026-02-28 に全項目実装完了。LMDB キャッシュ・中間 CTC バッチ化・GroupNorm・非同期チェックポイント・torch.compile (推論)。コミット `86ce5a5`。テスト数: 561 件。
> **Note**: SDPA 速度修正は 2026-03-02 に実装完了。`_forward_chunk_sdpa`（3-7x 速度低下）から `_forward_sdpa`（3.5x 高速化）にディスパッチを変更。CLI に `--use-flash-attention` 引数追加。コミット `6585523`。テスト数: 561 件。
> **Note**: P0+P1+Triton 訓練高速化は 2026-03-02 に全項目実装完了。sorted dynamic batching・torch.compile (訓練)・Triton RPE fused kernel (推論専用)・foreach grad clipping・intermediate CTC conditional skip・gradient checkpointing 制御。Triton backward が訓練時 37.9% 遅いため、訓練時は SDPA にフォールバック (`not self.training` 条件)。ベンチマーク: Manual 1163.7ms → SDPA+Triton推論 991.7ms/step (14.8% 高速化、メモリ -13.5%)。コミット `f0a61dd`。テスト数: 688 件。

### 訓練全体の推定改善効果

| カテゴリ | 改善率 |
|---------|--------|
| データパイプライン (GPU 利用率改善) | **5-8x** (最大要因) |
| Optimizer (fused AdamW) | **+37%** (step 時間) |
| DDP 通信 (バッチ化 + bucket 調整) | **+15-35%** (マルチ GPU) |
| in-place SiLU + contiguous 修正 | **+5-10%** |
| 推論 (inference_mode + FP16) | **+50-100%** |

---

## 2. データパイプライン — GPU 利用率 10-15% (最重大)

### 2.1 ボトルネック: pyopenjtalk PnP ラベル生成

- `generate_pnp_labels()` が **~5-15ms/サンプル**
- バッチサイズ ~410 サンプル、4 ワーカー → ~1000ms/バッチ
- GPU forward+backward: ~100-150ms/バッチ
- **データ準備 / GPU 時間比 ≈ 7-10x → GPU は常時データ待ち**

### 2.2 推奨対策

| 優先度 | 施策 | GPU 利用率 |
|--------|------|-----------|
| 現状 | num_workers=4, リアルタイム PnP 生成 | ~10-15% |
| P0 | `persistent_workers=True` (1 行) | +1-3% |
| P0 | `num_workers=8` (1 行) | ~20-30% |
| **P0** | **PnP ラベル事前キャッシュ (LMDB/WebDataset)** | **~70-80%** |
| P1 | `prefetch_count=8-16` | ~80-90% |

---

## 3. Optimizer / Scheduler

### 3.1 `fused=True` — 最重要 1 行変更

```python
# optimizer.py:52
torch.optim.AdamW(param_groups, lr=..., betas=..., fused=True)
```

- T4 実測: optimizer.step() **24.66ms → 15.37ms (37.7% 削減)**
- 1.2M ステップで **~5 時間削減**
- T4 (CC 7.5) で完全サポート済み

### 3.2 8-bit Optimizer (bitsandbytes)

- AdamW state: 640 MB → 160 MB (**480 MB 削減**)
- 要 `uv add bitsandbytes`
- T4 15 GB で余裕がある場合は不要

---

## 4. 訓練ループ最適化

### 4.1 即実装推奨

| 施策 | 効果 | 変更量 |
|------|------|--------|
| `reduce_metrics()` 3 回 → 1 回バッチ化 | +2-5% (マルチ GPU) | distributed.py |
| `find_unused_parameters=False` | +5-10% (マルチ GPU) | distributed.py |
| `persistent_workers=True` | +1-3% | trainer.py |
| `loss.item()` を 100 ステップごとに遅延 | +1-2% | trainer.py |
| `bucket_cap_mb=50` (DDP) | +5-15% (マルチ GPU) | distributed.py |
| 非同期チェックポイント保存 | save スパイク解消 | checkpoint.py |

### 4.2 マルチ GPU 合計改善: +15-35%

---

## 5. モデルモジュール最適化

### 5.1 Embedding / Upsample

| 施策 | 効果 |
|------|------|
| `repeat_interleave` → `expand(...).contiguous()` | **1.38x 高速化** (ゼロコスト) |
| `nn.Embedding(sparse=True)` | optimizer step 高速化 |
| bfloat16 embedding | -66.5 MB メモリ |

### 5.2 FFN (モデル計算量の 84%, パラメータの 40%)

| 施策 | 効果 |
|------|------|
| `F.silu(x, inplace=True)` | ~209 MB メモリ削減 |
| `torch.compile` (推論時) | +10-20% スループット |

### 5.3 Convolution

| 施策 | 効果 |
|------|------|
| **BatchNorm → GroupNorm** | DDP 通信削減 +3-8%, fp16 安定性改善 |
| ストリーミング cache pre-alloc | 推論 +10-20% |
| `cudnn.benchmark = True` | +5-15% DW Conv |

### 5.4 CTC Loss

| 施策 | 効果 |
|------|------|
| `.permute(1,0,2).contiguous()` 明示 | 隠れたコピー排除 |
| 中間 CTC 3 回 → 1 回バッチ化 | CTC 計算 10-25% 高速化 |

---

## 6. メモリ管理

### 6.1 即実装可能

| 施策 | 効果 | コスト |
|------|------|--------|
| **`PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`** | フラグメンテーション大幅低減 | 環境変数 1 行 |
| val 前後の `torch.cuda.empty_cache()` | OOM リスク低減 | 2 行 |
| `SiLU(inplace=True)` | 微小メモリ削減 | 2 行 |

### 6.2 中期的改善

| 施策 | 効果 |
|------|------|
| Intermediate CTC を checkpoint 内に含める | ~256 MB 活性化削減 |
| Attention-only selective checkpointing | 最大効率 |

### 6.3 非推奨

- **Activation offloading**: T4 の PCIe Gen3 (~16 GB/s) がボトルネック、逆効果

---

## 7. torch.compile

### 7.1 重要な互換性問題

**`torch.compile + DDP + gradient_checkpointing` の 3 者同時使用は PyTorch 2.x で重大バグあり。**

- Issue #144035: `CheckpointError: different number of tensors saved`
- Issue #118984: `crashes with DDP + dynamic shapes + checkpoint`

### 7.2 推奨適用戦略

| 場面 | 適用 | 理由 |
|------|------|------|
| **推論** | **適用推奨** (`reduce-overhead`) | +30-50% 速度改善, リスク低 |
| 訓練 (GC 無効) | 検討可 | メモリ増加トレードオフ |
| 訓練 (GC + DDP) | **回避推奨** | 重大バグあり |

> **Note**: 訓練時の torch.compile が FFN+ConvModule 個別 compile (`reduce-overhead` モード) として実装済み。DDP + gradient_checkpointing の3者同時使用バグを回避するため、エンコーダ全体ではなく個別モジュールを compile する方式を採用。

---

## 8. 推論パイプライン最適化

### 8.1 即実装推奨

| 施策 | 効果 | コスト |
|------|------|--------|
| `torch.no_grad()` → `torch.inference_mode()` | +5-15% | 数行 |
| FP16 推論 (`model.half()`) | +40-60%, VRAM 半減 | 数行 |
| 長さソートバッチング | +20-40% | pipeline.py |
| CTC decode ベクトル化 | +10-30% (大バッチ) | ctc_decoder.py |

### 8.2 デプロイメント

| 施策 | 効果 | 工数 |
|------|------|------|
| ONNX export (非ストリーミング) | ORT 推論可能 | 1-2 日 |
| TensorRT FP16 | 2-3x 速度向上 | 2-3 日 |
| INT8 量子化 (PTQ) | 3-4x 速度向上 | 2-3 日 |
| torch.compile (推論) | +30-50% | 数行 |

---

## 9. 実装優先度ロードマップ

### Phase 0: ゼロコスト改善 (即日実装, 数行変更) ✅ 全項目実装済み

1. ✅ `fused=True` (optimizer.py) — optimizer 37% 高速化
2. ✅ `expandable_segments:True` (環境変数) — メモリ安定化
3. ✅ `repeat_interleave` → `expand+contiguous` (embedding.py) — 1.38x
4. ✅ `F.silu(x, inplace=True)` (feed_forward.py, convolution.py) — ~209 MB 削減
5. ✅ `persistent_workers=True` (trainer.py) — ワーカー再起動排除
6. ✅ `.transpose(0,1).contiguous()` (cc_g2pnp.py) — 隠れたコピー排除
7. ✅ `torch.inference_mode()` (推論パス全般) — 推論 +5-15%

### Phase 1: 低コスト改善 (1-2 日) ✅ 全項目実装済み (コミット `d37a34b`, テスト 499 件)

1. ✅ `reduce_metrics()` バッチ化 (distributed.py) — DDP +2-5%; avg/sum グループ分けで最大 2 回の `all_reduce` に集約
2. ✅ `find_unused_parameters=False` (distributed.py) — DDP +5-10%; `wrap_model_ddp()` のデフォルト値として設定済み
3. ✅ `bucket_cap_mb=50` (distributed.py) — DDP 通信効率化; `wrap_model_ddp()` で `bucket_cap_mb=50` を指定済み
4. ✅ `loss.item()` 遅延 (trainer.py) — +1-2%; `train_step()` の戻り値として返し、ログ・pbar 更新時にのみ参照
5. ✅ val 前後 `empty_cache()` (trainer.py) — OOM 防止; バリデーション実行前後に `torch.cuda.empty_cache()` を追加
6. ✅ FP16 推論 + 長さソートバッチング (pipeline.py) — 推論 +50-100%; `_run_batch_inference()` で長さ順ソート後に `torch.amp.autocast(dtype=float16)` を適用
7. ✅ CTC decode ベクトル化 (ctc_decoder.py) — 推論 +10-30%; `greedy_decode()` で `unique_consecutive` + boolean mask によるベクトル化デコードを実装

### Phase 2: 中コスト改善 (3-5 日) ✅ 全項目実装済み (コミット `86ce5a5`, テスト 561 件)

1. ✅ **PnP ラベル事前キャッシュ (LMDB)** — `cc_g2pnp/data/lmdb_cache.py` + `scripts/preprocess_pnp.py` 追加、dataset.py 統合。GPU 利用率 10% → 70-80%
2. ✅ 中間 CTC バッチ化 — `cc_g2pnp/model/cc_g2pnp.py` 中間 CTC 損失を 1 回の call にバッチ化。CTC 10-25% 高速化
3. ✅ BatchNorm → GroupNorm — `cc_g2pnp/model/convolution.py` に `use_groupnorm` フラグ追加。DDP 通信削減 + fp16 安定性
4. ✅ 非同期チェックポイント保存 — `cc_g2pnp/training/checkpoint.py` に `async_checkpoint` フラグ追加
5. ✅ torch.compile (推論パイプライン) — `cc_g2pnp/evaluation/pipeline.py` に `use_compile` フラグ追加。※ FlashAttention Phase 1/2 は別施策として実装済み
6. ✅ **SDPA ディスパッチ修正** (コミット `6585523`) — `_forward_chunk_sdpa`（Python for ループ・1640 回 SDPA 呼び出し・T4 で 3-7x 速度低下）から `_forward_sdpa`（全系列 SDPA・単一呼び出し）に切り替え。T4 ベンチマーク: 1028ms → 290ms/step (3.5x 高速化)、メモリ -0.32GB。CLI に `--use-flash-attention` 引数追加

### Phase 2.5: P0+P1+Triton 訓練高速化 ✅ 全項目実装済み (コミット `f0a61dd`, テスト 688 件)

1. ✅ **sorted dynamic batching** — `collator.py` に `sorted_dynamic_batch_sampler` 追加。バッファ N サンプル → 長さソート → バッチ化でパディング 40-60% → 5-15% に削減
2. ✅ **torch.compile (訓練)** — FFN+ConvModule 個別 compile (`reduce-overhead` モード)。DDP+GC バグ回避のため個別モジュール方式
3. ✅ **Triton RPE fused kernel (推論専用)** — `triton_attention.py` で Q@K^T + Q@pos_K^T を単一カーネルで融合。訓練時は backward が 37.9% 遅いため `not self.training` 条件で SDPA にフォールバック
4. ✅ **foreach=True grad clipping** — 勾配クリッピングを 13% → ~2% に短縮
5. ✅ **Intermediate CTC conditional skip** — `disable_intermediate_ctc_after` パラメータで後半ステップの中間 CTC を省略 (5-10% 計算削減)
6. ✅ **Gradient checkpointing 制御** — `gradient_checkpointing` フラグで有効/無効切替
7. ✅ **Gradient accumulation** — `--gradient-accumulation-steps` CLI フラグ
8. ✅ **use_flash_attention CLI 対応** — `--use-flash-attention` で SDPA 有効化 (config デフォルト False, 明示指定が必要)
9. ✅ **use_groupnorm デフォルト True** — GroupNorm がデフォルト
10. ✅ **amp-dtype デフォルト float16** — T4 最適化

### Phase 3: 高コスト改善 (5-10 日)

1. ONNX export + TensorRT — 推論 2-4x
2. 8-bit optimizer (bitsandbytes) — 480 MB 削減
3. ✅ Gradient accumulation — `--gradient-accumulation-steps` CLI フラグ (Phase 2.5 で実装済み)

---

## 10. 調査エージェント一覧

| # | 担当 | エージェント | 状態 |
|---|------|-----------|------|
| 1 | Embedding/Upsample | embedding-opt | 完了 |
| 2 | Convolution | conv-opt | 完了 |
| 3 | FFN | ffn-opt | 完了 |
| 4 | CTC Loss | ctc-opt | 完了 |
| 5 | データパイプライン | data-opt | 完了 |
| 6 | torch.compile | compile-opt | 完了 |
| 7 | 訓練ループ | trainer-opt | 完了 |
| 8 | Optimizer/Scheduler | optim-opt | 完了 |
| 9 | メモリ管理 | memory-opt | 完了 |
| 10 | 推論パイプライン | inference-opt | 完了 |
